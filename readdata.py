#We want to take .mat files of multiple runs, combine them and then graph all of them/ averages of them.
#How we do this is first we create a super array of mat files.
#One for the original values and one for the updated time/pos

import scipy.io as sio
import numpy as np
